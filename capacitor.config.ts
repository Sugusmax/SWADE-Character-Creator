import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ChristianDev.SavageCreator',
  appName: 'SWADE Character Creator',
  webDir: 'dist'
};

export default config;
